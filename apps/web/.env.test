# vite `--mode test` 时加载。唯一作用:告诉 @cloudflare/vite-plugin 用 wrangler.jsonc 的
# `env.test` 段 + `.dev.vars.test`,从而把 Binance 请求指向本地假 server(e2e 用)。
#
# 这是 Cloudflare 官方推荐的做法 —— vite mode 与 Cloudflare environment 用一个开关串起来,
# 见 developers.cloudflare.com/workers/vite-plugin/reference/cloudflare-environments/
CLOUDFLARE_ENV=test

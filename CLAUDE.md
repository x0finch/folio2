# Folio — Engineering Conventions & Progress

> **Folio** is a self-hosted crypto portfolio tracker (on-chain wallets + CEX + perp DEX + manual assets → one dashboard). Full blueprint: [evolution/arch-design.md](evolution/arch-design.md). Read this file first every session, do only the current phase, then update the Progress block and stop.

## Tech stack
- TanStack Start (`@tanstack/react-start`) + Vite — full-stack, server functions
- Cloudflare Workers runtime; Cloudflare D1 (SQLite) + Drizzle ORM
- better-auth (Drizzle adapter) for auth
- pnpm workspace monorepo; TypeScript strict
- Vitest (test-first); Wrangler for deploy

## Core principles (non-negotiable)
1. **Contract-first** — define types & the `BalanceProvider` interface before implementations. Every provider implements the same interface.
2. **Test-first** — each provider adapter gets tests against recorded fixtures before its implementation. Parsing logic must have golden tests.
3. **Modular** — each provider is an independent package (`@folio/provider-*`, own package.json), interdependency-free, composed via the shared interface. UI lives in `@folio/ui`.
4. **Tests beside src** — each package's tests go in `tests/` (sibling of `src/`); provider API fixtures in `tests/fixtures/`.
5. **Secrets never leave / never echoed** — APIs return only `has*` booleans, never credential values. All credentials encrypted at rest (AES-GCM via Web Crypto, `SECRETS_KEY` from env).
6. **`@folio/db` exposes only wrapped ops** — no Drizzle instance / schema handle exported; only userId-scoped domain functions. All data access funnels through here.
7. **Relative imports without extensions** — `moduleResolution: bundler` workspace-wide; write `import './foo'`, never `./foo.js` or `./foo.ts`.
8. **No hardcoding** — chain IDs, RPCs, API bases, gap limits, derivation paths, timeouts, concurrency, TTLs → config/constants/env. Magic numbers must be named. Volatile env-specific values → env; stable domain constants → each package's `constants.ts`.
9. **Prefer mature, vetted libraries** — before each phase ask if a widely-adopted lib/standard exists (signing, BIP32, crypto, dates, decimals — don't hand-roll). A library must pass 4 gates or be rejected: ① runs on CF Workers (no Node-native deps, reasonable size); ② actively maintained, no known severe CVEs; ③ complexity matches payoff; ④ no conflicting transitive deps. Record the choice (what/why/rejected) in the commit or here.
10. **kebab-case filenames; exports keep their own convention** — files/dirs lowercase-hyphenated (`balance-table.tsx`, `create-account.ts`), decoupled from export names. Exports: React components PascalCase, functions/vars camelCase, types/interfaces PascalCase, constants UPPER_SNAKE.
11. **UI = shadcn design system only** — colors/spacing/radius/fonts via shadcn design tokens (CSS vars: `bg-background`, `text-foreground`, `border-border`, …). No hardcoded color values, no arbitrary-value classes (`bg-[#...]`, `p-[13px]`), no editing component internals. New visuals = compose existing shadcn components or adjust theme tokens.
    - **Base components are added via `shadcn add`, never hand-written.** Run `pnpm dlx shadcn@latest add <comp>` (monorepo-aware: primitives land in `@folio/ui`, blocks in `apps/web`) and export from `@folio/ui/src/index.ts`. Only **app-level compositions** (`apps/web/src/components/`) are authored by hand, by composing `@folio/ui` primitives. Do not transcribe shadcn source manually.
    - **Vendored shadcn components are not unit-tested** (they're upstream-maintained source). Validate the integration instead — the apps/web build must emit their utility classes (cross-package `@source`). Write tests for our own logic (app compositions, hooks), not for `@folio/ui` primitives.
12. **Small commits, English messages** — commit each acceptance-worthy unit; the message ties contract/test/impl together. **Commit messages are written in English** (planning/discussion happens in Chinese, but the git history stays English). Conventional-commit style: `type(scope): summary`.

## Package conventions (monorepo)
- **Packages are created on-demand by the phase that needs them** — not pre-stubbed. P1.2 creates `packages/core`, P1.4 `packages/db`, P1.5 `packages/ui`, each provider when built. The workspace globs in `pnpm-workspace.yaml` already cover `packages/*`, `packages/providers/*`, `apps/*`, so new packages need no config change.
- **Internal-packages pattern**: each `@folio/*` package.json sets `"exports": { ".": "./src/index.ts" }` pointing at source — **no build step** for internal libs (Vite/Vitest transpile TS directly). Consumers depend via `"@folio/<x>": "workspace:*"`.
- **No TS project references** (overkill here); each package `tsconfig.json` just extends `../../tsconfig.base.json`.
- **Each package ships its own minimal `vitest.config.ts`** + a `test: "vitest run"` script. This keeps `vitest` from inheriting the root `projects` config when run inside a package, and lets the root runner (`pnpm test:packages`) discover the package. The root `vitest.config.ts` (`test.projects`) is the canonical all-package runner.
- **A provider serving multiple account types**: keep `BalanceProvider.accountType` singular; use a factory to emit one provider object per type (shared impl), export `providers: BalanceProvider[]`, and let `@folio/sync` flatten them into `buildRegistry`. See arch-design.md §2 (方案 A).
- Package prefix `@folio/*`. Providers published as `@folio/provider-<name>`.

## Toolchain notes (current best practices — supersede older wording in arch-design.md)
- **`wrangler.jsonc`** (not `wrangler.toml`) — Cloudflare's recommended format; some features JSON-only. Wrangler v4.
- **Vitest 4** with root `test.projects` in `vitest.config.ts` (`vitest.workspace.ts` is removed in v4).
- **Cloudflare targeting via `@cloudflare/vite-plugin`** (not the legacy Nitro `cloudflare-module` preset).
- Package name is **`@tanstack/react-start`** (the old `@tanstack/start` is gone).
- TypeScript: `module: preserve` + `moduleResolution: bundler` (now the modern defaults) enable extension-less relative imports.
- **D1 testing**: `@cloudflare/vitest-pool-workers` 0.16.x — use the root `cloudflareTest` plugin + `readD1Migrations` (the old `defineWorkersConfig`/`/config` subpath is removed); `env`/`applyD1Migrations` from `cloudflare:test`; `Cloudflare.Env` augmentation in a test `env.d.ts`. This version doesn't isolate per-test storage → reset state in `beforeEach`.
- **D1 data layer**: D1 has no interactive `db.transaction()` → atomic multi-writes use `db.batch([...])`. D1 enforces FKs, so `ON DELETE CASCADE` works at runtime. Migrations: `drizzle-kit generate` (out=`drizzle/`) → `wrangler d1 migrations apply` (never `drizzle-kit migrate`); `out` == wrangler `migrations_dir` == test `readD1Migrations` path.
- TS6 generic `Uint8Array<ArrayBufferLike>` isn't assignable to Web Crypto `BufferSource`/`D1` BufferSource — annotate byte arrays as `Uint8Array<ArrayBuffer>` when needed.
- **better-auth on CF Workers** (1.6+): native `node:crypto` scrypt is auto-selected with `nodejs_compat` — no hash override needed (supersedes arch-design §7.1 ①). Build the auth instance lazily (`env` from `cloudflare:workers`), never at module load. `@better-auth/cli` (1.4.x) lags better-auth (1.6.x) and fails under this repo's jiti (pulls a stale `better-call`) → define the Drizzle auth schema by hand per the official spec. Verify auth via curl with an `Origin: http://localhost:3000` header (CSRF guard).
- **TanStack Start server routes**: `createFileRoute("/path/$").server.handlers` (GET/POST → `Response`). The `server` option is a Start augmentation of `@tanstack/router-core`; since app `src` doesn't import `@tanstack/react-start`, add `/// <reference types="@tanstack/react-start" />` (in `src/env.d.ts`) so `tsc` sees it. Run `wrangler types` after editing `wrangler.jsonc` bindings → `worker-configuration.d.ts` (Biome-excluded).
- **Lint/format = Biome** (`biome.json`, 2.x): `pnpm lint` (check) / `pnpm lint:fix` (write); CI runs `biome ci`. Style: 2-space, double quotes, lineWidth 100. Excludes vendored/generated (shadcn `components/`+`styles/`, `routeTree.gen.ts`, `drizzle/`, all `*.css`); `.gitignore` reused via `vcs.useIgnoreFile`. `noNonNullAssertion` is off in test files only. No ESLint (no type-aware lint yet; `tsc --strict` covers types).

## Security model (see arch-design.md §3, §7.1)
- Global provider keys (`ZERION_API_KEY`, etc.) → CF Secret/env. Per-account credentials → D1, AES-GCM encrypted with `SECRETS_KEY`.
- Read-only tracking, **no signing** → `ProviderCredentials` has no private-key field; on-chain accounts store address/xpub only.
- Decrypt only inside server functions at fetch time, discard immediately, never log.
- better-auth CF gotchas (apply in P2.1): native `node:crypto` scrypt hash override; single module-level auth instance; `ctx.waitUntil` for background tasks; secondaryStorage TTL ≥ 60s; disable cookieCache; no auth calls at module load.

---

## Progress

- [x] **P1.1 — monorepo skeleton** (workspace shell + `apps/web` + base configs + this file). Packages created on-demand thereafter.
- [x] **P1.2 — core contracts** (`packages/core`: `types.ts`, `provider.ts`, `registry.ts`, `errors.ts`, `index.ts` + tests). Contracts only; registry auto-assembles by `accountType`; multi-type via factory (方案 A).
- [x] **P1.3 — credential crypto** (`@folio/core/crypto.ts`: Web Crypto AES-GCM `encrypt`/`decrypt`, random 12B IV, `generateSecret`, `CryptoError`; key passed in by caller, never reads env; zero deps). Tests: round-trip/IV-randomness/wrong-key/tamper/invalid-key.
- [x] **P1.4 — Drizzle schema + `@folio/db`** (`schema.ts` business tables + indexes/cascade FKs; `getDb` private; `queries.ts` userId-scoped wrapped ops + `writeSnapshot` via `db.batch`; `index.ts` leaks no db/schema). Migrations in `drizzle/` (`drizzle-kit generate` → `wrangler d1 migrations apply`). Tests via `@cloudflare/vitest-pool-workers` (Miniflare D1): CRUD/M2M/cascade/isolation/encapsulation. **Deferred to P2.1**: better-auth tables, `userId→user` FK, `createAuthAdapter`. db stays crypto-agnostic (opaque ciphertext).
- [x] **P1.5 — `@folio/ui` shadcn + Tailwind v4** (`globals.css` owns Tailwind entry + theme tokens; `cn` + `Button` added via `shadcn add`; named re-exports; `components.json` ×2 drive the CLI). apps/web `styles.css` collapses to `@import "@folio/ui/globals.css"` + `@source`. Vendored shadcn components are not unit-tested; rendering verified by the apps/web build emitting their utility classes (cross-package `@source` works → not unstyled).
- [x] **P1.6 — CI** (`.github/workflows/ci.yml`: push(main)/PR → pnpm install --frozen-lockfile → typecheck → test → build; pnpm via `packageManager`, Node 22, store cache).
- [x] **P1.7 — Lint/format (Biome 2.x)** (`biome.json`: recommended lint + formatter, 2-space/double-quote/width-100; excludes vendored+generated+css; `noNonNullAssertion` off in tests). Root `lint`/`lint:fix` scripts; CI runs `biome ci` before typecheck. **M1 complete.**
- [x] **P2.1 — better-auth** (email+password, no verification). `@folio/db`: `auth-schema.ts` (user/session/account/verification, hand-defined — CLI 1.4.x lags 1.6 & breaks under jiti), `createAuthAdapter(env)` (drizzleAdapter, no db/schema leak), business `userId→user.id` FK now enforced (tests seed a user). `apps/web`: lazy single auth factory in `src/lib/auth.ts` (`env` from `cloudflare:workers`), route `routes/api/auth/$.ts` (`createFileRoute().server.handlers`), `tanstackStartCookies()`, `.dev.vars` (BETTER_AUTH_SECRET/URL). CF gotchas: ① native scrypt auto (1.6+nodejs_compat, no override) ②⑥ lazy singleton ⑤ cookieCache off ③ verify waitUntil at deploy. Verified locally: sign-up→sign-in→get-session 200 (needs `Origin` header).
- [x] **P2.2 — session guard + isolation** (`apps/web/src/lib/require-auth.ts`: `resolveAuth()` reads `getRequestHeaders()` → `getAuth().api.getSession`, throws 401 if none, returns `{userId,user,session}`; `requireAuth = createMiddleware({type:"function"}).server(...)` injects `context.userId`). Protected server fns originally used an `authedServerFn(opts)` builder wrapping `createServerFn(opts).middleware([requireAuth])` — **⚠️ removed in P2.5**: TanStack's compiler only strips a server fn's handler (and its server-only imports like `cloudflare:workers`) when it sees a literal `createServerFn().handler()` at the call site; a wrapper hides it, so once routes client-import these fns the server-only imports leak into the client bundle. Now each fn calls `createServerFn({method}).middleware([requireAuth]).handler(...)` directly. Security boundary = the server fn (not routes). Tests: `resolveAuth` unit (mock getSession) — 401 / userId-from-session; data-layer isolation via P1.4 D1 tests. `getWebRequest`/`getHeaders` were renamed to `getRequest`/`getRequestHeaders`.
- [x] **P2.3 — manual provider + 方案 B 契约演进** (`@folio/provider-custom`, serves `manual`). Refactored from "holdings-in-credentials" to **方案 B**: provider methods take `FetchContext { account, creds, globalKeys }` (not raw `(creds, globalKeys)`); per-account **non-secret** data lives on **`Account.data`** — a generic container (`type AccountData = ManualData`, union grows per type), kept out of `ProviderCredentials` (secrets only: apiKey/secret/passphrase/identifier). `@folio/core`: added `ManualData {holdings: ManualHolding[]}`, `AccountData`, `Account.data?`, `FetchContext`; removed `holdings?` from `ProviderCredentials` (so `hasHoldings` is gone). `customProvider.fetchBalances(ctx)` reads `(ctx.account.data as ManualData)?.holdings` → `Balance[]` (`kind:"manual"`); `validate(ctx)` checks shape. Exports `customProvider` + `providers[]` (方案 A flatten). Holdings stored **plaintext** (`dataJson` column) — they are **not secrets** and the derived numbers already sit plaintext in `snapshotBalances`; AES-GCM is reserved for `encCredentials` only (encrypting `data` while snapshots stay queryable-plaintext would be security theater). **Deferred**: db plaintext `dataJson` column + migration → P2.4; sync (`@folio/sync`) decrypt creds & assemble `FetchContext` + register providers → P2.4; account-creation / 录入 UI writing `data` → P2.5; auto-pricing (CMC/`@folio/pricing`). Tests: FetchContext-based fetchBalances/validate (fixture = `account.data`).
- [x] **P2.4 — sync orchestrator** (`@folio/sync`). `@folio/db`: `accounts.dataJson` (nullable **plaintext** JSON for non-secret `Account.data`) + migration `0002`; `CreateAccountInput.dataJson?`; `dataJson` rides in `AccountSafe`/`accountSafeColumns` (non-secret → returned with list/get, **no separate getter** — deviates from plan, which proposed `getAccountData`; cleaner + removes an N+1). `@folio/sync`: `registry.ts` (`appRegistry = buildRegistry([...customProviders])`, 方案 A flatten); `orchestrator.ts` with **injected db deps** (`SyncDeps {listAccounts, getEncryptedCredentials, writeSnapshot, secretsKey, globalKeys, registry?}`) so the pkg has no D1 → plain vitest. `runAccountSync` (pure: getProvider→fetchBalances→Σ usdValue), `syncAccount` (decrypt creds via `@folio/core`, parse `dataJson`→`data`, build `FetchContext`, writeSnapshot; whole-body try/catch → `{ok:false,error}`, never throws), `syncUser` (iterate, isolate). manual `encCredentials` = encrypted `{}`. `apps/web`: `src/lib/server/sync.ts` `triggerSync` (`authedServerFn` POST, binds `@folio/db` fns + `env.SECRETS_KEY` + `globalKeys:{}`, returns per-account ok/fail, no secrets). `.dev.vars`(.example) + `SECRETS_KEY`; `wrangler types`. **Crypto decision**: only `encCredentials` encrypted; `dataJson` + `snapshotBalances` plaintext (queryable; see arch §3). Tests (plain vitest + injected fake db + real core crypto): manual e2e snapshot / failure isolation / totalUsd + empty-snapshot. **Deferred**: account-creation/录入 UI writing `dataJson` → P2.5; concurrency+backoff → M3; global provider keys → M3+.
- [x] **P2.5 — minimal UI loop** (**M2 complete**). `@folio/ui`: `shadcn add input label card table` + named re-export. `apps/web`: auth-client (`src/lib/auth-client.ts`, `better-auth/react`, baseURL omitted → origin). Server fns — `createManualAccount` (writes plaintext `dataJson` + encrypted `{}` creds; validates holdings via `customProvider.validate`), `getMyOverview` (merges accounts with latest snapshots → `{rows, totalUsd}`), `fetchCurrentUser` (plain `createServerFn`, returns `{user}|null`, for the guard). Routes: `/login` (combined sign-in/sign-up), `_authed.tsx` (pathless layout, `beforeLoad` → `fetchCurrentUser` → `redirect("/login")` if none; nav + sign-out), `_authed/index.tsx` (overview), `_authed/accounts.tsx` (account list + manual-add form w/ dynamic holding rows + "Sync now"). Old top-level `index.tsx` removed (overview owns `/`). **Server-only-import discipline (key gotcha)**: server fns must use literal `createServerFn().handler()` (no wrapper — see P2.2 note); `getAuth`/`getRequestHeaders` confined to the `requireAuth` middleware's stripped `.server()`; pure `resolveAuth(result)` extracted to `src/lib/auth-session.ts` (no server imports → unit-testable + not pulled into client bundle). Decisions taken (per plan): combined login page / defer `has*` to M4 / validate holdings before save. Runtime-verified via curl: login 200, unauth `/`→307 /login, sign-up creates user+session, authed `/` & `/accounts`→200. Gates: typecheck/test(web 2, packages 38)/biome/build all green. **Deferred**: on-chain/CEX/perp account UIs (M3/M4), history chart (P6.1), groups UI (P6.2), i18n (P6.4), settings/export (P6.5).
- [x] **P3.1 — zerion provider** (`@folio/provider-zerion`, serves `onchain_evm`). `GET /v1/wallets/{address}/positions/` (`filter[trash]=only_non_trash&currency=usd`) → tokens + DeFi across all EVM chains in one call. **HTTP Basic** (key as username, empty password); address from `ctx.creds.identifier`, key from `ctx.globalKeys.ZERION_API_KEY`; native `fetch`, **zero deps**. Pure `parsePositions(json)→Balance[]` (IO-separated, golden-tested): skip `flags.displayable=false`/no-symbol; `position_type!=="wallet"||protocol`→`defi` else `spot`; null `value`→0; chain/protocol/positionType → `Balance.meta`, `source=chain`. `validate` = format precheck (no request) + lightweight `portfolio` endpoint probe (2.6KB vs positions' 14MB). Errors → `ProviderError` (401/403 AUTH_FAILED, 429 RATE_LIMITED-retryable, 5xx/net UPSTREAM_ERROR, bad JSON PARSE_ERROR, missing addr/key INVALID_CREDENTIALS). Registered in `@folio/sync` `appProviders`; `triggerSync` injects `env.ZERION_API_KEY` into `globalKeys`; `.dev.vars`(.example)+`ZERION_API_KEY`, `wrangler types`. **Contract unchanged** (`onchain_evm`/`identifier`/`Balance.meta`/`globalKeys` all pre-existing). Fixture = curated real-schema (verified against a live `vitalik.eth` dump). Tests: 9 — parse golden is a full-output `toEqual` of the `Balance[]` (spot/defi, null value→0, chain/protocol→meta, skip displayable=false) + fetch (mocked) error codes + validate. Live-verified with real key: positions 200, portfolio 200. **Deferred**: on-chain account entry UI → P3.3. Gates: typecheck/test(packages 60, web 2)/biome/build green.
- [x] **least-privilege global keys** (post-P3.1). `BalanceProvider` gains `readonly usesGlobalKeys?: readonly string[]` (default none). `@folio/sync` `syncAccount` scopes `FetchContext.globalKeys` to that subset (`scopeGlobalKeys`) — a provider can't read other providers' global keys (e.g. zerion can't see CMC/exchange keys). zerion declares `usesGlobalKeys: [ZERION_API_KEY]`; manual declares none → `{}`. Required-vs-optional is the provider's own concern (it throws or degrades), not the contract's. Tests: core shape (declare field), sync (declared key delivered, undeclared withheld, none → `{}`). Gates: typecheck/test(packages 63, web 2)/biome/build green.
- [x] **P3.2 — on-chain account UI** (EVM). `apps/web`: `createOnchainEvmAccount` server fn (`createServerFn POST + requireAuth`) — regex-prechecks the `0x`+40hex address (clear error, no request), then **live `zerionProvider.validate`** (lightweight portfolio probe; requires `ZERION_API_KEY` + reachable Zerion) before save; stores `encCredentials = encrypt({ identifier: address })`, **no `dataJson`**, `type: "onchain_evm"`. Pure helpers in `src/lib/onchain.ts` (`normalizeEvmAddress`, `buildEvmCredentials`) — no server-only imports → unit-testable. UI: accounts page gains an "Add on-chain wallet (EVM)" card (label + address); no network input (Zerion is multi-chain); list shows label+type only (address encrypted). Reuses `triggerSync`/`getMyOverview` unchanged (type-agnostic). apps/web adds `@folio/provider-zerion` dep. Decisions: per-type creation fn / live validate up front / separate card. Tests: web 5 (onchain address validation + creds round-trip). Live-verified with real key (throwaway test): validate→true, fetchBalances→non-empty. **Deferred**: `onchain_solana` form (needs P3.3 coinstats), overview chain breakdown, has* (M4). Gates: typecheck/test(packages 63, web 5)/biome/build green.
- [x] **zod server-fn input validation** (post-P3.2). Server fns' `.validator()` was an identity passthrough (TS-only, no runtime check). Now uses **zod schemas** (`.validator((d:unknown) => Schema.parse(d))` — version-robust form) for real boundary validation + type inference: `createManualAccount` (label + holdings shape), `createOnchainEvmAccount` (label + `EVM_ADDRESS_RE`). Division of labor: **zod = wire/shape validation; provider.validate = liveness/domain** (zerion's live probe stays; manual's now-redundant `customProvider.validate` call dropped — zod fully covers manual shape). `src/lib/onchain.ts` slimmed to `EVM_ADDRESS_RE` + `buildEvmCredentials` (dropped `normalizeEvmAddress`). Added `zod@4` to apps/web. Gates: typecheck/test(packages 63, web 5)/biome/build green.
- [x] **P3.3 — coinstats provider** (`@folio/provider-coinstats`; **first multi-type provider**, 方案 A factory). Serves `onchain_solana`/`onchain_sui`/`onchain_cosmos`. `GET /wallet/balance?address=&connectionId=` with `X-API-KEY` (free tier); each coin carries `amount`+`price(USD)` → `usdValue=amount*price`; spot-only endpoint → `kind:"spot"`. Native `fetch`, zero deps. Pure `parseBalances(coins, chain)→Balance[]` (golden-tested: skip no-symbol, null price→0, chain/connectionId/contract → meta). `makeCoinstats(accountType, connectionId)` factory (declares `usesGlobalKeys:[COINSTATS_API_KEY]`); `CONNECTION_IDS` map confirmed via live `/wallet/blockchains` — **Sui is `sui-wallet`, not `sui`**; solana=`solana`, cosmos=`cosmos`. `validate` = non-empty addr+key → live `wallet/balance` probe (3 address formats differ → left to the API). Registered in `@folio/sync` `appProviders` (**first real 方案 A flatten**: one package → multiple provider objects); `triggerSync` injects `COINSTATS_API_KEY`; `.dev.vars`(.example)+key, `wrangler types`. Contract unchanged. Tests: 9 — parse **golden** is a full-output `toEqual` of the `Balance[]` (covers Solana+Sui+Cosmos, usdValue=amount*price, null price→0, per-coin chain/connectionId) + factory multi-type/connectionId + mock-fetch error codes + validate. Fixture's Cosmos entry is **real** (from a user-provided `cosmos1…` address — confirms cross-chain schema parity); Sui entry is schema-faithful. Live-verified with real key (throwaway): validate→true, fetchBalances→non-empty spot. **Deferred**: Sui/Cosmos/Solana address-entry UI (per-chain formats) → later UI phase. Gates: typecheck/test(packages 85, web 5)/biome/build green.
- [x] **perf(db) — `getLatestSnapshotByUser` constant-query** (drop N+1). Was `1 + 2M` queries (per-account loop: latest snapshot + its balances). Now **2 queries** regardless of account count: ① latest snapshot per account via a `GROUP BY accountId MAX(takenAt)` subquery joined back to `snapshots` (scoped to the user via `⨝ accounts`), ② all those snapshots' balances via `inArray(snapshotId, ids)`, then group in JS. Same signature/return → `getMyOverview` unchanged. JS dedup guard for the (effectively impossible) same-ms tie. Added composite index `snapshots(account_id, taken_at)` (migration `0003`, additive `CREATE INDEX`). Tests: db +2 (multi-account/multi-snapshot picks latest + balances; empty → `[]`). Gates: typecheck/test(packages 79)/biome/build green.
- [x] **P3.4 — unified on-chain account entry UI** (**M3 complete**). Replaced the EVM-only card/fn with a single **"Add on-chain wallet"** card: chain `Select` (Ethereum/EVM · Solana · Sui · Cosmos) + label + address. `shadcn add select` → `@folio/ui`. Generic `createOnchainAccount({type, label, address})` server fn: zod `type` **whitelist** (`onchain_evm`/`solana`/`sui`/`cosmos`; EVM gets `EVM_ADDRESS_RE` via `.refine`, others non-empty → format left to provider/API), dispatches via `getProvider(appRegistry, type).validate` (live probe) with keys scoped by `scopeGlobalKeys(...)` (now exported from `@folio/sync`) to the provider's `usesGlobalKeys` (least-privilege at creation too), stores `encrypt({identifier})`, no `dataJson`. `onchain.ts`: `buildEvmCredentials`→`buildAddressCredentials` (chain-agnostic). Live-verified (throwaway): solana dispatch → scoped to COINSTATS_API_KEY only (no Zerion leak) + validate→true. **Bitcoin (xpub) deferred** (no provider). Gates: typecheck/test(packages 79, web 5)/biome/build green.
- [x] **P3.5 — sync retry/backoff + Retry-After** (revised down from "concurrency + token-bucket": an in-process bucket is NOT a real global limiter on CF Workers' multi-isolate runtime, and concurrency is premature at single-user scale — true global RPS enforcement = Durable Object, deferred to M6/cron). Scale-matched + API-citizen approach instead: `@folio/core` `ProviderError` gains optional `retryAfterMs` + `parseRetryAfter(header)` (numeric secs / HTTP-date → ms). zerion/coinstats parse `Retry-After` on 429 → `retryAfterMs`. `@folio/sync` `withRetry` wraps the **provider call only** (not decrypt/writeSnapshot): on `retryable` ProviderError, wait `retryAfterMs ?? min(RETRY_MAX_MS, RETRY_BASE_MS·2^(n-1))` + jitter, up to `RETRY_MAX_ATTEMPTS=3`; non-retryable / exhausted → bubbles to the per-account `{ok:false}` isolation. `sleep` injectable via `SyncDeps.sleep?` (default setTimeout) → deterministic tests. Sync stays **sequential** (no concurrency, no `BalanceProvider.rateLimit` field — avoids a "declared but unenforced" illusion). Tests: core errors (retryAfterMs/parseRetryAfter), zerion 429→retryAfterMs, sync (retry-then-succeed honoring Retry-After / give-up→ok:false / non-retryable not retried). Gates: typecheck/test(packages 86, web 5)/biome/build green.
- [x] **P4.1 — binance provider** (`@folio/provider-binance`, serves `exchange_binance`; **first CEX — HMAC read-only signature template**). **Per-account creds** (`ctx.creds.apiKey`/`secret`, encrypted) — NOT a global key → no `usesGlobalKeys`; `triggerSync` globalKeys unchanged. `sign.ts`: `hmacSha256Hex(secret, query)` via Web Crypto (zero deps), golden-tested against the openssl reference HMAC. `GET /api/v3/account` (SIGNED, USER_DATA; `X-MBX-APIKEY` header + `recvWindow`+`timestamp` query, `&signature=<hex>`). Binance balances are **amounts only (free/locked), no USD** → valued via the **public unsigned** `GET /api/v3/ticker/price` (asset→USDT): pure `parseAccountBalances(account, prices)` (free+locked, skip ≤0, stablecoin≈1, no-pair→0, `kind:spot`/`source:binance`). Errors → `ProviderError`: 401/403 AUTH_FAILED, **418/429 RATE_LIMITED + `parseRetryAfter` (Binance sends Retry-After → P3.5 retry honors it)**, 5xx UPSTREAM_ERROR, missing creds INVALID_CREDENTIALS. Registered in `@folio/sync` `appProviders`. Spot only (funding/earn deferred; **futures positions = perp/M5**). Tests: 8 (sign golden / parseAccountBalances golden incl valuation / fetch mocked + error codes / validate). Live-verified with a real read-only key (throwaway): signing→200, validate→true, fetchBalances→non-empty valued spot. **CEX account-entry UI (key/secret, return only `has*`) → P4.6.** Gates: typecheck/test(packages 103→111 w/ binance 8, web 5)/biome/build green.
- [x] **shared HMAC + P4.2 okx provider** (`@folio/provider-okx`, serves `exchange_okx`; second CEX). **Extracted `hmacSha256(secret, msg, "hex"|"base64")` to `@folio/core`** (Web Crypto, golden-tested vs openssl for both encodings); binance now calls it directly (its redundant `sign.ts` wrapper + test removed — one HMAC primitive, tested once). OKX differs from binance: ① sign = **base64**(HMAC(`timestamp+'GET'+requestPath`)), 4 headers `OK-ACCESS-KEY/SIGN/TIMESTAMP(ISO8601)/PASSPHRASE` — **needs passphrase** (`ctx.creds.passphrase`); ② `GET /api/v5/account/balance` `details[]` carry **`eqUsd`** → USD valuation built-in (no ticker pricing); ③ **error model: HTTP 200 + `code!=="0"`** (incl. key/sign errors) → `assertCodeOk` maps auth codes (50111/50113/50102/50104/50105/…) → AUTH_FAILED, else UPSTREAM_ERROR; HTTP 429 → RATE_LIMITED. Pure `parseBalances(details)` (eq→amount, eqUsd→usdValue, skip ≤0, kind:spot/source:okx). Per-account creds → no `usesGlobalKeys`. Registered in `@folio/sync`. Tests: core hmac (hex+base64 golden) + okx 7 (parse golden / 4-header base64 sign / **200+code auth-error mapping** / 429 / missing-creds); binance trimmed to 6 (signing now in core). Live-verified with a real read-only key+passphrase. Gates: typecheck/test(packages 116, web 5)/biome/build green.
- [x] **P4.6 — CEX account entry UI** (binance/okx; done early, before P4.3–P4.5 per priority change). `apps/web`: `createExchangeAccount` server fn — zod `type` whitelist (`exchange_binance`/`exchange_okx`, only providers that exist), `apiKey`/`secret` required, `passphrase` optional with `.refine` (OKX requires it); live `getProvider(appRegistry, type).validate` (signed read-only probe) before save; stores `encrypt({apiKey, secret, passphrase?})` (real secrets, encrypted), no `dataJson`. Pure `buildExchangeCredentials` in `src/lib/exchange.ts` (no server imports → unit-tested round-trip). UI: third "Add exchange account" card — exchange `Select` (Binance/OKX) + label + API key + secret (`type=password`) + passphrase (password, **only when OKX**). Reuses `triggerSync`/`getMyOverview`. CEX has no `usesGlobalKeys` → `scopeGlobalKeys` gives `{}`; creds via `ctx.creds`. **"Never returned"**: `listMyAccounts` returns `AccountSafe` (no `encCredentials`); `has*` display deferred. Tests: web 7 (`buildExchangeCredentials` round-trip ± passphrase). Gates: typecheck/test(packages 116, web 7)/biome/build green. **bybit/bitget/gate providers (P4.3–P4.5) still pending.**
- [x] **P5.1 — hyperliquid provider** (`@folio/provider-hyperliquid`, serves `perp_hyperliquid`; **first perp DEX**). **Address-only, no auth/API key** (closest to an on-chain provider): `POST https://api.hyperliquid.xyz/info` body `{type:"clearinghouseState", user}`; address from `ctx.creds.identifier` (EVM `0x`+40hex), **no `usesGlobalKeys`**, `triggerSync` globalKeys unchanged. Native `fetch`, zero deps. **Perp ≠ spot model** (decision 1①): a `Balance`'s `usdValue` sums into the portfolio total, but a perp position is leveraged exposure — using notional as `usdValue` would inflate the total. So pure `parseClearinghouseState(state)→Balance[]` emits **one equity row** (`symbol:"USDC"`, `usdValue=accountValue` — the only value-bearing row, feeds total; `meta.role:"equity"` + withdrawable/totalMarginUsed/totalNtlPos) **+ one row per position** (`usdValue:0` so no double-count; `symbol=coin`, `amount=szi` signed, `meta.role:"position"` + side(long/short from szi sign)/entryPx/positionValue/unrealizedPnl/leverage/liquidationPx/marginUsed). All HL numeric fields are **strings** → `Number()`. `kind:"perp"`, `source:"hyperliquid"`. `validate` = address format precheck (no key needed) + one live `clearinghouseState` 200 probe (untraded address also 200 → treated usable). Errors → `ProviderError`: missing/bad addr INVALID_CREDENTIALS, 429 RATE_LIMITED + `parseRetryAfter`, 5xx/net UPSTREAM_ERROR, bad JSON PARSE_ERROR. Registered in `@folio/sync` `appProviders` (方案 A flatten). Tests: 11 — `parseClearinghouseState` **golden** full `toEqual` (equity + long/short positions, string→Number, side, null liquidationPx; empty-account → equity-only; total == accountValue) + fetch (mocked POST body/error codes/PARSE_ERROR) + validate. Fixture = schema-faithful (official example + a synthetic short), **no real holdings**. Live-verified: real endpoint 200, shape matches interface. Decision 2①: **spot deferred** (HL spot balances give amounts only; valuation needs extra `spotMetaAndAssetCtxs`/`allMids` calls + `@N` pair mapping → own phase). **Perp account-entry UI + display refinement (side/PnL/leverage) → P5.4.** Gates: typecheck/test(packages 138, web 7)/biome/build green.
- [x] **P5.4 — perp entry UI + display + perp-meta contract** (**M5 complete** for hyperliquid). Closes P5.1's two best-practice debts. **Contract (`@folio/core`)**: added `PerpEquityMeta`/`PerpPositionMeta`/`PerpMeta` next to `Balance` — the shared shape for `kind:"perp"` meta so producer (provider) and consumer (overview) stop being stringly-typed on both ends (原则 #1). P5.1 provider refactored to build meta as inline literals with `satisfies PerpEquityMeta/PerpPositionMeta` (compile-time checked; fresh literal still fits `Balance.meta: Record<string,unknown>` with no cast — avoids the `as unknown as` noise). **Entry**: `createPerpAccount` server fn (zod `type` whitelist `perp_hyperliquid`, `address` `.refine(EVM_ADDRESS_RE)`, live `getProvider().validate`, `buildAddressCredentials` encrypt `{identifier}`, no `dataJson`) — mirrors `createOnchainAccount` (decision 1①, separate fn not generalized). UI: 4th "Add perp account" card (venue `Select` Hyperliquid + label + address). **Display** (decision 2①): pure `toPerpView(balances)→{equity, positions[]}` in `src/lib/perp.ts` (no server imports → unit-tested) parses each row's `metaJson` and narrows via `meta.role` (JSON boundary cast in one tested place). Overview route branches by category (`account.type.split("_")[0]==="perp"`): perp → `PerpPositions` (equity headline = card's `totalUsd`; subline Withdrawable/Margin used; table Coin/Side/Size/Entry/uPnL/Lev/Liq.; negative uPnL `text-destructive` — token only, 原则 #11), else `SpotTable` (unchanged). **Decision 3 revised**: meta parsed in `toPerpView` (client pure fn), NOT server-side — TanStack server-fn return must be JSON-serializable and a `Record<string,unknown>` (unknown-valued) fails that constraint; parsing in the tested pure fn keeps the component pure anyway. `getMyOverview` returns balances with `metaJson` as-is. Tests: web 10 (+3 `toPerpView`: equity/positions split, empty account, bad/missing metaJson tolerated); P5.1 golden unchanged (meta keys identical). Gates: typecheck/test(packages 138, web 10)/biome/build green. **P5.2 derive · P5.3 extended deferred** (user skipped; copy hyperliquid template + reuse `PerpMeta`/`toPerpView`/perp card).
- [x] **P6.1 — portfolio history chart** (first M6 phase). **db**: `listSnapshotTotalsByUser(env, userId) → {accountId, takenAt, totalUsd}[]` (snapshots ⨝ accounts user-scope, `asc(takenAt)`, **no balances** — lighter than `getLatestSnapshotByUser`). **Pure** `buildPortfolioHistory(rows) → {t, total}[]` in `src/lib/history.ts` (no server imports → unit-tested): the portfolio total at time T = Σ each account's most-recent snapshot ≤ T, since accounts' `takenAt` don't align — so **stepwise reconstruction** (asc walk, running `accountId→latest totalUsd` map, emit a point at each timestamp; same-`takenAt` multi-account events merged into one point). Naive per-timestamp bucket-sum would undercount accounts not synced in that bucket (✗). **server fn** `getPortfolioHistory` (GET + requireAuth) calls the pure fn and returns `{series}` (numeric → JSON-serializable, unlike perp meta, so built server-side). **UI**: `shadcn add chart -c packages/ui` (Recharts v3 → `@folio/ui` `ChartContainer`/`ChartTooltip…`, exported). App composition `src/components/portfolio-chart.tsx` (原则 #11 — composes `@folio/ui` + Recharts primitives; colors via `--chart-1` token, no hardcoded hex). Overview route loads overview+history via `Promise.all`, renders a "Portfolio value" card on top; `series.length < 2` → "Not enough history yet" (no empty chart). `recharts@3.8.0` added to apps/web deps. Tests: db +2 (`listSnapshotTotalsByUser` asc/user-scope/empty), web +4 (`buildPortfolioHistory` stepwise/same-ts merge/sort/empty). Chart component not unit-tested (vendored shadcn + pure display; build emits its classes). Gates: typecheck/test(packages 140, web 14)/biome/build green. **Deferred**: by-account/by-group series toggle (by-group needs P6.2); time-range selector (needs P6.3 cron density).
- [x] **Position model hardening (route ①)** — establishes the contract skeleton for mixing heterogeneous positions (exchange spot/earn, perp equity+positions, on-chain DeFi LP/lending/staking) in one account while showing each faithfully. Two pillars: **(A) net-value invariant** — `account.totalUsd === Σ balances.usdValue`, where `usdValue` is the **signed net contribution** (debt negative; exactly one value-bearing row per economic position, the rest `0` — e.g. perp equity bears value, positions `0`; LP pool bears value, underlying `0`). Documented as a must-follow contract comment on `Balance` in `@folio/core`. **(B) typed meta family + sectioned rendering** — generalizes P5.4's `PerpMeta`: added `DefiMeta {chain?, protocol?, positionType?}` to `@folio/core` (anchored to zerion's actual output; lending/LP/earn detail fields + a normalized `positionType` enum deferred to each provider as it conforms). Pure `toAccountSections(balances) → {spot[], defi: GroupedByProtocol[], perp: PerpView|null}` in `src/lib/account-view.ts` (no server imports → unit-tested); overview account card renders **per-kind sections** (spot table + DeFi grouped-by-protocol + perp positions) instead of the old "perp-or-spot" branch. Negative `usdValue` (debt) shown `text-destructive`. **First conformers (no new provider built)**: perp (already) + **zerion DeFi** — zerion already emits `kind:"defi"` + `{chain,protocol,positionType}`, now annotated `satisfies DefiMeta` (producer-side compile check; golden unchanged). Visible win: zerion DeFi positions, previously dumped into the flat spot table, now render as their own protocol-grouped section. Chose route ① (harden flat `Balance`, zero DB migration) over ② (dedicated `Position` discriminated union — heavier; revisit at the view layer when position types explode). Tests: web +4 (`toAccountSections` spot/defi-grouping/perp/empty). Gates: typecheck/test(packages 140, web 18)/biome/build green. **Deferred**: per-kind meta detail fields + `positionType` normalization (fill as each provider lands).
- [x] **P6.2 — groups (many-to-many)**. db M2M ops already existed (P1.4); added bulk `listMembershipsByUser(env,userId)→{accountId,groupId}[]` (`account_groups ⨝ accounts` user-scope, one query — no per-account N+1). **server fns** (`lib/server/groups.ts`): `getMyGroups` (→ `{groups, memberships}`), `createGroup`/`deleteGroup`/`addAccountToGroup`/`removeAccountFromGroup` (zod + requireAuth; folded away the unused `listMyGroups`). **Pure** `toGroupedView(rows, groups, memberships)` in `src/lib/groups-view.ts` (no server imports → unit-tested): groups sorted by `sortOrder`→`name`; **invariant** — a multi-group account counts in *every* group's subtotal (intended), `ungrouped` = accounts in no group, and the **grand total is NOT Σ subtotals** (would double-count) — the top `totalUsd` from `getMyOverview` (deduped per account) stays the source of truth. **UI**: Accounts page gains a "Manage groups" card (create/delete) + per-account group checkboxes (toggle → add/removeAccountToGroup); Overview gains a "By group" card (per-group subtotal + member labels + Ungrouped) shown only when groups exist; account detail cards unchanged (decision ①/① — in-page management + summary section, no card re-render per group). Tests: db +2 (`listMembershipsByUser` multi/scope/empty), web +4 (`toGroupedView` multi-group/ungrouped/order/empty). Gates: typecheck/test(packages 142, web 22)/biome/build green. **Deferred**: group rename, drag-reorder (`sortOrder` column exists), by-group history series.
- [ ] **M6 cont.** ← **next**: P6.3 cron · P6.4 i18n · P6.5 export/settings. **Deferred**: M4 P4.3 bybit/P4.4 bitget/P4.5 gate; M5 P5.2 derive/P5.3 extended; `requiredCredentials` refactor — provider declares its required creds (binance `[apiKey,secret]`, okx `+passphrase`) so the app *derives* the passphrase field instead of the hardcoded `exType === "exchange_okx"` (post-P4.6 note; unrelated to perp meta); P6.1 history series by-account/by-group toggle + time-range selector.
